namespace Odusseus.Rules.Model.Enumeration
{
    public enum Level
    {
        Debug = 100,
        Trace = 90,
        Warning = 80,
        Info = 70,
        Quiet = 60,
        Error = 50,
    }
}

